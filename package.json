{
  "name": "claude-telegram-mcp-multi-agent",
  "version": "1.0.0",
  "description": "Telegram channel MCP server for Claude Code multi-agent setups",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "claude-telegram-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "claude",
    "claude-code",
    "mcp",
    "telegram",
    "multi-agent",
    "channels"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
